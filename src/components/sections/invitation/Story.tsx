import { motion } from 'framer-motion';
import event from '../../../data/event.json';
import { Section } from '../../shared/Section';
export function Story() { return <Section id="historia" eyebrow="Tres años de bendiciones" title="Su presentación" tone="ivory"><div className="story-grid"><motion.img src={event.storyImage} alt={`Ambientación de la fiesta de ${event.name}`} loading="lazy" initial={{ opacity: 0, scale: .96 }} whileInView={{ opacity: 1, scale: 1 }} viewport={{ once: true }} /><div className="story-copy"><span className="quote-mark">“</span><p>{event.story}</p><span className="signature">{event.name}</span><div className="family"><div><span>{event.parents.label}</span><strong>{event.parents.father}</strong><strong>{event.parents.mother}</strong></div><div><span>{event.godparents.label}</span>{event.godparents.names.map(name => <strong key={name}>{name}</strong>)}</div></div></div></div></Section>; }
