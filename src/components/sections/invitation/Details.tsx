import { CalendarDays, Church, Clock3, Gift, MapPin, PartyPopper } from 'lucide-react';
import event from '../../../data/event.json';
import { Section } from '../../shared/Section';
const details = [{ icon: CalendarDays, label: 'Fecha', value: event.dateLabel }, { icon: Clock3, label: 'Misa de acción de gracias', value: event.timeLabel, sub: `${event.church.name} · ${event.church.address}` }, { icon: MapPin, label: 'Fiesta infantil', value: event.location.name, sub: event.location.address }, { icon: PartyPopper, label: 'Tema de la fiesta', value: event.party.theme, sub: 'Con brincolín, juegos y mesa de dulces para los peques.' }, { icon: Gift, label: event.giftTable.name, value: event.giftTable.detail, link: event.giftTable.url }];
export function Details() { return <Section id="detalles" eyebrow="Todo listo para celebrar" title="Detalles del evento"><div className="details"><article className="place-card"><Church /><span>{event.church.label}</span><h3>{event.church.name}</h3><p>{event.church.address}</p><a href="#ubicacion">Cómo llegar →</a></article><div className="details-list">{details.map(({icon: Icon, label, value, sub, link}) => <article className="detail" key={label}><Icon /><div><span>{label}</span>{link ? <a href={link} target="_blank" rel="noreferrer">{value} ↗</a> : <strong>{value}</strong>}{sub && <small>{sub}</small>}</div></article>)}</div></div></Section>; }
